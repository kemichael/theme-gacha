def team_schema(team):
    return {"id": team.id, "name": team.name}

def theme1_schema(theme):
    return {"id": theme.id, "name": theme.name}

def theme2_schema(theme):
    return {"id": theme.id, "name": theme.name}